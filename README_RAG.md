# 🧠 Antigravity Global RAG Memory System

**Ваш персональний "Другий Мозок" для AI Агентів.**

Це готова, автономна система довготривалої пам'яті (Long-Term Memory), яка дозволяє AI агентам (у VS Code, на серверах, у чат-ботах) запам'ятовувати факти, контекст та уподобання користувача і ніколи їх не забувати.

---

## 🚀 Чому це Game Changer?

### Для VS Code + Antigravity (та інших AI асистентів)

Ви помічали, що AI "забуває", про що ви говорили вчора? Або щоразу треба нагадувати "я використовую Python, а не JS"?
Ця система вирішує це. Вона створює **Persistent Context Layer**:

* **Міжсесійна пам'ять:** Перезавантажили VS Code? Не проблема. Агент пам'ятає все.
* **Глобальні знання:** Один агент навчився чомусь в проекті А — агент в проекті Б вже це знає.
* **Приватність:** Всі дані живуть локально у вас на диску. Ніякої хмари.

### Для розробників (Інтеграція в будь-які проекти)

Це не просто плагін для VS Code. Це повноцінний **Memory Microservice**.
Ви можете використовувати цю пам'ять у своїх Python скриптах, Telegram ботах, веб-сайтах.

* **Stand-alone:** Працює як окремий HTTP сервіс.
* **Universal API:** Просто шліть JSON-запити.

---

## 🛠 Як це працює "після капотом"?

Система побудована на сучасній **Client-Server архітектурі** для максимальної швидкості та стабільності.

### 1. Векторні Embeddings (The "Magic")

Замість тупого пошуку за ключовими словами (Ctrl+F), система використовує **семантичний пошук**.

* Ми використовуємо модель `all-MiniLM-L6-v2`.
* Текст перетворюється на вектор (масив з 384 чисел), що кодує *сенс*.
* *"Dog"* та *"Puppy"* будуть поруч у векторному просторі, хоча букви різні.

### 2. Рівні Доступу (Scopes)

Унікальна фішка цієї системи — поділ пам'яті:

* 🟢 **Global:** Знання доступні всім вашим агентам у всіх проектах (напр., "Користувач любить темну тему").
* 🟡 **Local:** Знання, специфічні для конкретного проекту (напр., "В цьому проекті використовуємо Poetry").
* 🔴 **Private:** Особисті нотатки, які бачить тільки агент-власник.

### 3. Архітектура Мікросервісів

Система складається з двох компонентів, що спілкуються по HTTP:

* **Port 8000 (Database):** ChromaDB сервер. Зберігає вектори. Гарантує, що дані не поб'ються при одночасному доступі.
* **Port 8001 (Brain):** Embedding сервер. Тримає AI модель у пам'яті. Завдяки цьому пошук займає **50 мілісекунд** (а не 60 секунд на завантаження моделі).

---

## 📦 Встановлення (Out-of-the-Box)

Система запакована так, щоб розгорнутись за 1 хвилину на будь-якій машині з Windows/Linux/Mac (з Python).

1. **Скопіюйте папку** `rag` на цільову машину.
2. **Запустіть інсталятор:**

   ```powershell
   # Windows
   .\install_rag.ps1
   ```

   *(Він встановить Python-залежності та налаштує конфіги)*

---

## 🚦 Запуск та Використання

Перед роботою натисніть "червону кнопку" (запустіть сервіси):

```powershell
.\start_rag_services.ps1
```

*Відкриється вікно, де видно статус Database та AI Model. Не закривайте його.*

> [!NOTE]
> **Час старту (Warmup Time)**
> При першому запуску (або на слабших ПК) системі може знадобитись **30-60 секунд**, щоб завантажити нейромережу в пам'ять.
> Ви побачите повідомлення `🔥 Warming up model...`.
> Це одноразова дія при старті. Після цього всі запити будуть миттєвими.

### Інтеграція в Antigravity / MCP

Додайте цей блок у ваш `mcp_config.json` (шлях підкаже інсталятор):

```json
"antigravity-rag": {
  "command": "python",
  "args": [".../path/to/rag/server.py"]
}
```

### Інтеграція у Власний Проект (Python приклад)

Не хочете MCP? Стукайте напряму в базу!

```python
import chromadb

# Підключаємось до нашого RAG сервера
client = chromadb.HttpClient(host='localhost', port=8000)
collection = client.get_collection("antigravity_global_knowledge")

# Пошук (потрібно генерувати embeddings - див. документацію API)
# Але найпростіше використати наш клієнт-обгортку 'chroma_client.py'
```

### Масштабування

* Хочете перенести базу на потужний сервер? Просто запустіть `chroma run` на сервері та змініть `localhost` на IP сервера в конфігах.
* Хочете потужнішу AI модель? Змініть модель в `embedding_server.py`.

---

**Розроблено в Antigravity. Portability by Design.** 🚀
